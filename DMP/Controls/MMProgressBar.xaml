<UserControl x:Class="DMP.Controls.MMProgressBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="40" d:DesignWidth="210"
             DataContext="{Binding RelativeSource={RelativeSource Self}}"
             >
        <Grid>
        <Canvas Background="Yellow" Width="210" Height="40">
            <TextBlock Text="AAAA"/>
            <!--
            <Slider Name="slider1"  Width="200" Canvas.Left="5" Canvas.Top="10"
                    Value="1000" 
                    Maximum="{Binding MaxValue}"
                    ValueChanged="slider1_ValueChanged" 
                    IsSelectionRangeEnabled="True" 
                    SelectionStart="1000" SelectionEnd="1200" 
                    SmallChange="1"
                    Height="25" />
            -->
            
        </Canvas>
    </Grid>
    </UserControl>